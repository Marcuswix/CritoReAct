import React from 'react'

const SectionTitle = ({title, headline}) => {
  return (
    <>
        <div>
        <h4>{title}</h4>
        <h1>{headline}</h1>
        </div>
    </>
  )
}

export default SectionTitle